[project]
name = "verity-engine"
version = "0.1.0"
description = "Local Polars and DuckDB data-plane engine for Verity"
requires-python = ">=3.11"
dependencies = [
  "duckdb>=1.3.0",
  "polars>=1.32.0",
  "pyarrow>=20.0.0",
  "pydantic>=2.11.0",
]

[project.optional-dependencies]
dev = [
  "jsonschema>=4.25.0",
  "pytest>=8.4.0",
  "ruff>=0.12.0",
]
orchestration = [
  "dagster>=1.11.0",
  "dagster-webserver>=1.11.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
